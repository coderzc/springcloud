spring:
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        shared-dataids: gateway.yaml # 共享配置
        refreshable-dataids: gateway.yaml # 支持自动刷新的配置
        group: DEFAULT_GROUP # 可以用来区分多环境